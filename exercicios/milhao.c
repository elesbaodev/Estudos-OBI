#include <stdio.h>

int main(void) {
    int n,a;
    scanf("%d%d", &n,&a);
    int valor[n];
    if(a >= 10000) {
        printf("1\n");
    } else {
          for(int i = 1; i < n; i++) {
        scanf("%d", &valor[i]);
        printf("%d", valor[4]);
        
        
       
        
    }
    }

return 0;
} 