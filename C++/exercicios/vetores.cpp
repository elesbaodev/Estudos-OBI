#include <iostream>

using namespace std;

int main(void) {
    int n;
    cin >> n;
    int vec[40];
    for(int i = 0; i < n; i++) {
        cin >> vec[i];
 
    } for(int i = 0; i < n; i++) {
            cout << vec[i] << endl;
        }
        return 0;
}