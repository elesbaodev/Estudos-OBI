#include <stdio.h>

int main(void) {
    int n,l,c;
    scanf("%d", &n);
    int vet[0][n];
    for(int i = 1; i <= n; i++) {
        scanf("%d",&vet[0][i]);
    } if () {
        printf("%d\n", c);
    }
}