#include <stdio.h>
#include <string.h>

int main(void) {
    // - STRLEN -- //
    /*char n1[100];
    scanf("%s", &n1);
    printf("%d", strlen(n1));
 return 0; */ 

 //-- STRSTR -- //
    /*char s[100];
    char c;
    
    scanf("%s %c", s, &c); // Leio a entrada
    
    int total = 0;
    
    for(int i = 0 ; i < strlen(s) ; i++) {
        if(s[i] == c) // Checo se c está em s, 
            total++; // se sim incremento total
    }

    printf("%d\n", total); // Exibo a resposta

    return 0;*/

    
}